mutation CreateOrderItem(
	$quantity: Int!
	$total: Int!
	$productId: ID!
	$orderId: ID!
) {
	createOrderItem(
		data: {
			quantity: $quantity
			total: $total
			product: { connect: { id: $productId } }
			order: { connect: { id: $orderId } }
		}
	) {
		id
	}
}
